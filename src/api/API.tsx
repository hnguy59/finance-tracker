import errorHandler from "api/errorHandler";
import * as commBankProduct from "api/services/endpoints/CommBankProductEndpoints";

const API = {
  commBankProduct,
  errorHandler,
};

export default API;
